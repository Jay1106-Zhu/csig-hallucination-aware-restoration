# 执行日志

## 2026-09-15 Phase 0.5 全量公开发布
- 用户明确要求“和上次一样，全部上传”，现授权本轮Git提交/push和新公开Release；沿用Jay1106-Zhu/csig-hallucination-aware-restoration。
- 已核实远端public、main=404991c2821293f6693d4174041d0273001f49f7，旧phase0-2026-09-15有7个公开资产，保持不动。
- 本轮约2.464GiB实验产物，另补真实配对原图/GT和冻结评价/编码器权重；仅排除依赖安装树、缓存杂项与凭据。只修改公开导出副本的本机路径，原实验保持不变。

## 2026-09-15 Phase 0.5
- 计算部分已完成：68组运行、324个轻量checkpoint，全部分组隔离；保存OOF、逐原图指标、三seed结果、分域及synthetic→real检查。
- 10%–100% risk–coverage、固定50%实际融合及三种oracle完成；275张融合图+mask已逐像素校验。
- 已生成89张可视化；查看风险曲线、case3全图和文字/海鸥/植被/夜景缺失对照，图表未将NA填零。
- 33项测试通过；独立审计1447个只读旧文件、220个候选、1160区域、324个checkpoint/OOF、3520条逐图指标和55个感知指标抽样复算通过；110个盲样本的抽样与图片哈希一致，关键重建误差0。
- 主模型V7宏AUC0.4050（仅5图定义），LQ-only0.6647；固定50%损害差+0.0087且区间跨0；预测实际融合PSNR仍比LQ低2.0892dB。最终HOLD，不进入Phase1。
- 补齐人工盲标注界面/协议与110条pending CSV，但没有真实人的输入，人工幻觉性能仍未完成；这不是可由助手编造补齐的步骤。
- 最终报告、阶段索引、分类特征/权重索引、全量本地哈希清单已归档，核心交付约2.464GiB；本轮不提交/推送GitHub，不变更Phase0历史结论。
- 55×4 候选哈希及原生尺寸校验完成；1160 个非重叠有效区域的 PSNR/SSIM/LPIPS/DISTS 与全图指标已落盘。
- 110 个固定盲标注样本、离线 HTML 与 CSV 导入器完成；无人审核的 severity 保持空白。
- 20 项基础/指标/分组/标注/特征契约测试通过；多尺度 DINO/CLIP/结构/敏感性提取运行中。
- 已完整阅读 CSIG_Phase05_Codex_Prompt(1).md；当前授权只覆盖新阶段，不自动发布 GitHub、不启动 Phase 1。
- 核实 Phase 0 原始结论和输入/候选/模型缓存，采用只读复用避免重复恢复。
- 数据扩展候选已定位 Berkeley BSDS500 官方镜像的 train 子集；拟构建可追溯合成退化对并与五张 CSIG 真实验证图分层报告，不能称为新增真实配对场景。
- 人工 hallucination 真值必须由人提供；本阶段会生成盲标注协议/工具/子集，不将模型或助手标注伪装成人工真值。
- 10 项基础契约测试先失败后全部通过；DISTS-pytorch 已隔离安装，本机已有 LPIPS/VGG 权重缓存。
- 55 个配对 scene manifest 已完成，原阶段只读快照已记录；正在对50个新输入运行四组官方 controlled sensitivity 条件，旧5图复用对应历史输出。

## 2026-09-15 GitHub 发布
- 范围变更：用户明确要求 Phase 0 指导要求的内容全部上传，取消此前对原图/GT 对照、特征和 checkpoint 的发布排除；改用 Git + Release 完整交付。
- 用户明确授权为 new_formal 创建新的 public 仓库。
- 确认 GitHub 账号 Jay1106-Zhu；Git Credential Manager 已有此账号，不读取或展示凭据。
- 拟定仓库名 csig-hallucination-aware-restoration，账号下无同名仓库。
- 本地约 3.3 GB，大部分为无损 patch、预训练权重及热图数组；发布时保留本地，只提交审查后的轻量内容。
- 新范围采用完整 Git + Release：五个归档包含所有 Phase 0 实际产物，另增加 validation 原始 LQ/GT 副本。
- 发布检查发现 pip freeze 含构建机器的 file:// 路径，已加入回归测试并转换为实际安装版本；不丢弃环境信息。
- 修复测试依赖父目录名 new_formal 的导入问题，独立仓库根目录也能运行 15 项原实验测试；未修改模型逻辑。
- GitHub MCP 创建仓库遭 403（此连接的权限不足），改用本机已登录的 Git Credential Manager 身份成功创建 public 仓库；未输出凭据。
- main 首次提交 23de9f6 已推送，Git 包含 107 个文件，全部可视化图片已公开；正在上传 5 个 Release ZIP，共 1434.43 MiB，245 个包内成员。
- 扫描器最初把自身测试字符串误判为 file:// 依赖，已收紧为真实 requirements 行并通过回归测试；未绕过实际敏感内容扫描。
- 大文件 Python HTTP 传输速度异常慢；用 16 MiB 可视化归档实测原生 curl 上传约 2.24 MB/s 且远端 SHA-256 一致，切换为原生 HTTPS 传输。认证头仅经内存/stdin 传递，不写入文件或命令行参数。
- 五个完整归档及两份校验清单已公开，7 个资产的远端 SHA-256 均匹配；匿名仓库/Release 访问以及实际校验文件下载均验证通过。
- 发布后修正验收记录中的草稿临时下载地址为正式 tag 下载地址；不修改已上传文件。
- 最终文档 Git push 遇连接重置（连续两次），公开的初始仓库与完整 Release 不受影响；正在排查传输并完成同步。
- 已定位 Git 没有沿用本机代理，使用单次命令的现有本机代理完成推送；未改全局 Git 配置，也未关闭 TLS 验证。
- 最终完整交付：公开 main、公开 Release、五个 ZIP + 两个清单全部可用；测试 15+7 项通过，所有远端归档哈希一致。
- 仓库和本地目录保持对应，验收入口 publication/GITHUB_DELIVERY.md；后续仅按新授权推进。

## 2026-09-15 Phase 0
- 已阅读两份新方案和旧计划上下文，建立独立记录。
- 尚未修改数据、HYPIR 或启动训练。
- 已固定 Phase 0 协议与参数；先写 12 项契约测试，观测缺少实现的预期失败后编写核心函数。
- 缺失 scikit-learn 已连同 joblib/threadpoolctl 安装在本阶段 dependencies 内，不替换旧环境包。
- 主实验选择官方 model_t=200/coeff_t=200，重新执行官方入口，不依据本轮 GT 挑选历史最优系数。
- 12 项核心契约测试已全部通过；环境与数据清单已归档，确认 5 对 validation、100 张 test 元信息。
- 官方 HYPIR 已后台运行，原始代码 Git 状态仅有预先存在的 models/weights 未跟踪目录，无代码改动。
- CLIP 和 DINOv2-small 预训练权重已下载到本阶段 models/，固定具体 revision 并记录文件 SHA-256。
- 5 张官方推理完成，347.41 秒；patch 3565×3 组已无损保存；CLIP/DINO 提取 68.77 秒；六组特征×5折×3seed 的 MLP 训练完成（55.37 秒）。
- 20 个失败案例、五组 gain/confidence 热图与逐图 ROC 已生成；一次可视化调用超时但产物实际完成，后续核验而非重复运行。
- 原始输入/GT、官方源码与恢复权重哈希校验全部一致；正在做独立产物审计与人工观察。
- 完成人工查看 20 个失败案例和 5 个补充语义区域，保存逐例标注及观察范围限制。
- 最终 15 项契约测试通过。独立审计校验全部 3565 组三源 patch 和标签，重建 90 个折 checkpoint，重算 OOF 与逐折指标；最大概率重建差 2.98e-08。
- 源数据/源码/恢复权重共 35 个文件保持原哈希；CLIP/DINO 权重未改变；test 参与训练/评价数量为 0。
- 已生成中文最终报告、phase0_done、总 README、阶段 README 和 NEXT_TASKS；不修改两份原始方案，不启动后续阶段。
- 最终结论：探索门槛 YES，实践 HOLD。主 AUC=0.719535，但只有三个有效原图且单正例图像抬高均值；不应直接进入 Gate 训练。
