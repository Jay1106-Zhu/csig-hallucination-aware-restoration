# Phase 0.5 visualizations

The figures are generated by `scripts/visualize.py`. Panels labelled pending are intentional: independent human severity labels have not been supplied. Token heatmaps show only the central processor field of view (224 pixels for a 256 context; 448 for a 512 context); they do not imply full-context token coverage.
