# Phase 0.5 剩余事项与后续前置条件

**HOLD。以下待办不是自动开训或发布授权。** 所有后续文件继续归入 `new_formal` 对应阶段，Phase0历史产物只读。

## A. 当前阶段无法由助手替代的任务

1. 真实人盲审 `data/annotations/index.html` 中固定110个区域，导出CSV，记录审核者ID/日期；不能把模型或助手判断冒充人。
2. 严重度无法判断就留空，不用0填充。建议第二位人独立审核同组，并留存两个版本。
3. 运行 `scripts/annotations.py --import-csv` 导入；`scripts/analyze_results.py` 当前可生成审核子集的质量/人类目标关联描述统计。
4. 在人工标签覆盖足够后，再补逐图混淆、相关/一致性、类型分层、hallucination-rate风险曲线、severity图和人类目标分组verifier；当前NA面板不是这些分析的替代品。

## B. 下一轮研究所需数据

- 补真实、独立、可追溯成对数据，不以同图patch或合成多版本冒充新scene。
- 解决当前只有13个PSNR正例、5个有效AUC原图的问题；先固定候选条件与采样协议，不据现有GT调参追分。
- 补真实夜景/flare、中文、人物等语义×退化交叉覆盖，先审核来源权限和配准；保留真实/合成分域。
- 对边缘可变大小区域，同时报告区域及像素coverage；若下一轮改为固定像素预算，应预先固定，并不能当作本轮原协议结果。

## C. 当前禁止自动启动

- Phase1恢复器/LoRA/MoE/专家/Gate训练。
- 根据当前OOF挑最佳seed、coverage、模型或系数后宣称独立验证成功。
- 无明确发布范围及来源许可核对就把本轮2GB级数据/特征/权重混进旧Phase0 Release。
